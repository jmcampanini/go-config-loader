# Slice values

Shows how slice fields are loaded from defaults, TOML, environment variables, and pflags. The example demonstrates comma-separated values, repeated singular pflags, and deduping while preserving order.

Run it with:

```sh
go run .
```
