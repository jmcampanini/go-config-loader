# Basic loading

Shows the core loading flow without a CLI framework: defaults are layered with a TOML file and environment variables, then provenance is printed for each field.

Run it with:

```sh
go run .
```
